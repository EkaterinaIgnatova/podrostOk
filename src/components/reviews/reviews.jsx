export const Reviews = () => {
  const reviews = [];

  if (!reviews.length) {
    return (
      <p style={{ textAlign: "center", opacity: "0.7" }}>
        Список отзывов пуст.
      </p>
    );
  }

  return <></>;
};
