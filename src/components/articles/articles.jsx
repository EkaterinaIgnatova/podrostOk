export const Articles = () => {
  const articles = [];

  if (!articles.length) {
    return (
      <p style={{ textAlign: "center", opacity: "0.7" }}>Список статей пуст.</p>
    );
  }

  return <></>;
};
